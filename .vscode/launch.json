{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "blazorwasm",
      "request": "launch",
      "name": "localhost (Blazor WASM)",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "localhost (Chrome)",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    }
  ]
}